# COPE CS 职业生涯模拟器 - 设计规范

## 设计目标

这是一个 Counter-Strike 职业生涯模拟器的操作界面，而不是传统体育生涯模式。它应像赛事后台、战队数据终端与选手档案系统的结合：冷静、紧凑、信息优先，用户始终围绕签约、训练、比赛、转会和荣誉做决策。

初始页面只负责建立选手档案并开始游戏。职业中心和荣誉档案属于生涯完结后的结果页面，不应出现在初始页面导航或内容区。后续页面必须保留统一的顶栏、状态栏、居中主内容和技术状态页脚。

## 视觉基调

- 不使用球衣、球场、足球号码、足球位置图、比分牌或任何传统体育俱乐部视觉隐喻。
- 采用战术终端风格：炭黑底、低饱和战术青、荧光黄状态色、警示橙行动色、细线网格和准星/数据矩阵元素。
- 图形元素只能服务于 CS 信息表达，例如服务器信号、准星、战术矩阵、武器偏好、赛事赛程和数据图表。
- 不使用装饰性光球、圆润的大型卡片、营销式英雄区或高饱和渐变。

## 设计令牌

| 名称 | 值 | 用途 |
| --- | --- | --- |
| `--ink` | `#101415` | 页面主背景 |
| `--carbon` | `#171d1e` | 深色表面 |
| `--panel` | `#1c2525` | 面板与弹窗 |
| `--cream` | `#eef0e8` | 主文本 |
| `--muted` | `#94a39e` | 次级文本 |
| `--acid` | `#c7ed4a` | 已选状态、正向数据、进度 |
| `--orange` | `#ef6c42` | 主行动、警示、关键节点 |
| `--cyan` | `#78c7c1` | 网络、战术与次级高亮 |
| `--line` | `rgba(208,225,215,.16)` | 边框与分隔线 |

字体：

- 标题：`Barlow Condensed`，700-800。
- 数据标签与微文案：`IBM Plex Mono`，500-600，全大写英文或简短中文。
- 中文正文和控件：`Noto Sans SC`，500-700。
- 不使用负字距；标题可使用约 `0.5px` 字距，数据标签可使用约 `0.7px`。

## 页面框架

- 顶栏：桌面端 57px，移动端 52px。左侧品牌，中部最少导航，右侧为模拟状态和语言。
- 状态栏：49px。展示模式、赛季、引擎状态和少量工具。
- 内容区：最大宽度 1440px，水平内边距 16-29px。
- 页脚：40px 技术状态条，只保留引擎版本、运行环境和版权。
- 背景仅使用低对比度的网格和炭黑层次，避免产生草坪或球场观感。

## 基础组件

### 面板

面板只用于实际工作内容：选手档案、合同邀约、赛事结果、转会决策、训练安排或荣誉档案。

- 半透明深色背景，`1px solid var(--line)` 边框。
- 最大圆角 3px。
- 内边距 15-18px。
- 面板标题行可含 25px 橙色步骤标识、标题、右侧数据代码。

### 按钮

- 主命令：橙色矩形、等宽字体、简短动词。适用于“开始生涯”“确认签约”“进入比赛”“提交选择”。
- 已选项：荧光黄背景和深色文本，用于角色、赛区、节奏或偏好武器。
- 次级命令：透明背景、荧光黄边框和荧光黄文字。
- 工具按钮可以仅图标，但需加 `title`。
- 除语言筛选外，不使用胶囊形文字按钮。

### 输入与选择器

- 标签为 9-10px 等宽字体、低饱和色。
- 控件使用深色半透明填充、细边框、固定高度。
- 互斥选项使用网格并明显呈现已选状态。
- 移动端关键控件保持至少 44px 高的触控面积。

### 数据行

- 主信息使用浅色文本，元信息使用 9-10px 等宽字体。
- 正向信息用荧光黄，风险或不可逆决策用橙色。
- 战队/赛事行的顺序：徽标、名称、排名或合同信息、右侧小操作。

## CS 专属界面

### 选手档案

- 头像固定使用 `assets/top/blankplayer.svg`，置于档案框中。不能以球衣、背号或运动服替代头像。
- 初始页只保留游戏 ID、真实姓名、主力位置和起始赛区等开始游戏所必需的信息；出生年份、语言、武器偏好等字段应放到后续设置或事件中。
- 角色选择使用“CS 五人职责列表”，包含突破手、狙击手、指挥、辅助、自由人。每行显示编号、位置、英文缩写、回合职责和队伍需求，选中项使用左侧荧光黄标线，不使用球场或体育场地布局。
- 首页只保留生涯模式与“开始生涯”主按钮。每日挑战、职业中心和荣誉档案在对应系统解锁后再出现。
- 生涯模式只有两个选项：`硬核模式`（资源紧张、失败会留下长期代价）与 `爽文模式`（高光事件、成长和机遇更充足）。模式选择应写入启动配置；未接通引擎前不得把它表现为已实际影响赛事或事件结果。

### 生涯中心

生涯中心只在开始游戏后出现。显示当前职业阶段：自由选手、青训、区域赛事、Tier 2、Tier 1、Major 争冠。展示当前战队徽标、角色、Rating、ADR、K-D、经济状况、士气、精力、当前日期和下一项可执行事件。

### 合同与转会

每份邀约必须包括：战队徽标、当前全球排名/赛事层级、首发或替补定位、合同年限和关键操作。初始排名可参考 `assets/teams/standings_global_2026_07_06.md`。

战队徽标路径来自 `assets/teams/teams_profile/`；将徽标放在 44px 浅色方形容器中，使用 `object-fit: contain`，内部最大 37px。缺少素材时保留容器并显示文字状态，不要压缩布局。

### 赛事与比赛模拟

桌面端建议两栏：左侧为当前赛事/比赛叙事与决策，右侧为赛季时间线或赛事列表。主要数据为地图、比分、Rating、K-D、ADR、首杀、残局、经济和道具影响。

模拟过程使用荧光黄进度和等宽状态文案，例如“正在读取对手 Demo”“赛后复盘完成”。

### 荣誉档案

荣誉档案只在生涯结束后的总结页面出现。

- MVP 槽位预留 `assets/mvp/copper_mvp.webp`、`assets/mvp/sliver_mvp.webp`、`assets/mvp/golden_mvp.webp`，固定为 78px 容器。
- Major 与 S 级赛事使用 `assets/events/` 中资源，图片容器固定为 56-72px。
- 年度 Top 20 使用 `assets/top/` 中 SVG。
- 未解锁荣誉保持原素材位置，以灰阶和低透明度表达；解锁后恢复原始颜色，并展示日期、赛事名和一行关键数据。

## 弹窗规范

使用原生 `<dialog>`：深色面板、荧光黄边框、炭黑遮罩。弹窗本身是唯一浮层，不要在弹窗内再嵌套卡片。

推荐结构：

1. 橙色等宽眉题，例如“转会窗口 / 需要决策”。
2. 32-40px 紧凑标题。
3. 一段简短情境说明。
4. 数据区：战队、合同年限、月薪、首发机会、世界排名、潜在风险。
5. 二至三个选择；推荐或不可逆命令使用橙色。
6. 右上角图标关闭按钮。

## 响应式规则

- 901px 以上：三列档案设置，支撑内容两列。
- 561-900px：档案和赛区两列，战术矩阵跨两列。
- 560px 以下：全为单列；隐藏长导航和次级状态，但保留所有关键操作。
- 字号不随视口宽度缩放。文本无法容纳时换行或调整布局，不得重叠。
